# WI-162 — Token snap label and Alt hint

SPEC-054 §8. Part of Batch 2a (WI-160, WI-162, WI-163) — one pull request, one combined
summary.

**Changes made**

- `apps/web/src/lib/components/CharacterDock.svelte` — the Character sheet's
  `token-snap-mode` control's label changes from "Snap" to "Token snap", with a new
  one-line hint below it: "Hold Alt while dragging to place freely." A `.hint` style
  (small, dim text, matching the existing `.group-label` dimming) is added for it. The
  control's values, `data-testid`s and behavior are unchanged.
- `README.md` — a note under "Map ⇄ character sheet (II.5)" documents the label and hint.

**Visible behavior changes** — the token-snap dropdown on the Character quick sheet reads
"Token snap" instead of "Snap", with a hint line underneath explaining the Alt-drag
override.

**How to verify** — `pnpm verify` green (lint, typecheck, unit). Manually: open the
Character quick sheet's Map defaults section, confirm the control reads "Token snap" and
the hint appears beneath it.

**Deviations** — none.
