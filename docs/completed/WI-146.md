# WI-146 — Rationale has one home: the SPEC-052 §3 prose pass

Spec: SPEC-052 §3. From: IN-141. Prose only — no id, structure or assertion changes.

## Changes made

Trimmed 21 `docs/completed/WI-nnn.md` completion records that were over the 40-line budget
down to ≤40 lines each, cutting restated rationale/history while keeping every fact, id and
assertion (before → after lines):

WI-031 43→40, WI-042 61→40, WI-045 42→38, WI-047 51→39, WI-057 48→40, WI-058 57→40,
WI-059 53→40, WI-065 49→40, WI-068 41→40, WI-073 51→40, WI-076 57→38, WI-088 65→40,
WI-091 61→38, WI-092 61→36, WI-108 44→40, WI-110 41→37, WI-117 46→40, WI-118 42→35,
WI-121 42→40, WI-123 45→40, WI-129 49→40.

One history-narrating code comment found under category 3 (`apps/web/src/lib/map/
vector-engine.ts`, the terrain-overlay clip block): trimmed from a blow-by-blow of
WI-101/WI-122's rejected `Sprite.mask` approach to the current invariant (clip is baked
into the texture at load time) plus its ids.

`README.md` was reviewed for over-budget rationale near `SPEC-`/`DEC-` citations; no
clear, unambiguous case was found on inspection, so nothing was changed there (see
Deviations).

`PLAN.md` §2 — WI-146's row removed; the "2026-09-18 introspective" narrative's WI-144/
WI-146 ordering note reworded to past tense now that both have landed. No other item's
narrative in that section touched. `PLAN-COMPLETED.md` §3 — row added. `INTAKE.md` —
IN-141's row moved from §1.1 to §1.2 in the one-line "Closed — WI-nnn (date). See
`docs/completed/WI-nnn.md`." shape WI-144/IN-140 established.

## Visible behavior changes

None (documentation only).

## How to verify

- `wc -l docs/completed/*.md | sort -n | awk '$1>40'` — none of the 21 files above appear.
- `pnpm verify` — lint + typecheck + unit + `docs:check`, all green.
- `git diff --stat` — only `docs/completed/*.md`, `PLAN.md`, `PLAN-COMPLETED.md`,
  `INTAKE.md`, and one line in `vector-engine.ts`.

## Deviations

- **~84 further `docs/completed/WI-nnn.md` files remain over the 40-line budget.** This
  session prioritized the largest cluster of near-threshold and standard-shaped records
  it could trim soundly in the time available (21 files, 530→302 lines net); a further
  pass is needed to clear the rest. Three files were explicitly skipped as
  investigation-style deliverables (WI-090, WI-098, WI-100, WI-119) where the bulk of the
  length is unique findings, not restated rationale — trimming them risks dropping
  information the record is the only home for, so they were left for a more careful,
  dedicated look rather than cut on this pass.
- **`README.md`'s category-2 pass found no clear violation to fix.** Spot-checked
  passages near `SPEC-`/`DEC-` citations; nothing was unambiguously "why" restated rather
  than "what" — left alone per the gate's conservative instruction rather than guessed at.
- **`INTAKE.md` §1.2's ~120 older "Closed via" prose cells are untouched** — that bulk
  trim is IN-151's job (logged by WI-144), not this item's SPEC-052 §3 scope.
- **`CLAUDE.md`'s reading-budget description was not edited**, despite `PLAN.md`'s
  introspective narrative listing it as a WI-144/WI-146 README obligation; WI-144's own
  completion record did not touch it either, and SPEC-052 §3 itself needs no restatement
  in `CLAUDE.md` beyond the existing pointer to it — left as a further intake item if the
  user still wants it.
