# WI-088 — `RULE-AMENDMENT`: RULE-009 states the backend per build

**Spec:** — (rule) · **From:** IN-065 · **Agent:** `claude-code` · **Model:** `opus` ·
**Effort:** medium · **Closed:** 2026-08-18

RULE-009 stated the backend as a single fact, contradicting IN-065's proposed local mode
before it could be designed (DEC-074). Standalone `RULE-AMENDMENT:` commit per RULE-017 —
the shape WI-037 (RULE-006) and WI-065 (RULE-010) took. No `LocalStore`, build mode or
lobby here; those are WI-089.

**Changes made.**

- `RULES.md` — RULE-009 is now **"Backend, per build"**. The hosted build's backend is
  restated **word for word**, unchanged. A **local** build is admitted beside it: no
  backend, identity provider or network; the campaign is a `.vttcamp` file backed store
  (SPEC-041); no Firebase SDK/project identifier in its output; explicitly not an offline
  mode, cache or sync layer. Two paragraphs state consequences: **what the split scopes**
  (RULE-003/004/011/012 scoped to the hosted build, none weakened) and **what it
  strengthens** (RULE-001 — local store passes the same contract suite; RULE-014 —
  locally the `.vttcamp` is the database). RULE-010 noted unaffected. Recorded in the
  house `> **Amended by WI-nnn (date).**` form.
- `docs/spec/SPEC-041.md` — header's **Blocked on WI-088** → **Unblocked 2026-08-18**.
- `PLAN.md`, `PLAN-COMPLETED.md` — ledger moves; WI-089's gate cell reads **Unblocked**.

**Visible behavior changes.** None. Only `RULES.md` and the ledger changed.

**How to verify.**

- `pnpm verify` — lint, typecheck, unit, quiet on success.
- `git show --stat`: five markdown files, no file under `apps/`, `packages/` or `firebase/`.
- `RULES.md` → RULE-009 reads "Backend, per build", carries the `Amended by WI-088` note.
- `docs/spec/SPEC-041.md` line 5 no longer says "Blocked on WI-088".
- Commit message carries the `RULE-AMENDMENT:` prefix RULE-017 requires.

**Deviations.**

- **`INTAKE.md` is unchanged; IN-065 stays in §1.1.** Its disposition is "SPEC-041,
  WI-088–089" and WI-089 has not run; moving it now would record it closed prematurely.
- **`README.md` is unchanged.** No local build exists yet — the amendment makes room for
  one rather than shipping one; README is WI-089's to update.
