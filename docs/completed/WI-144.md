# WI-144 — One home per fact: the retroactive pass

Spec: SPEC-052 §§1–2. From: IN-140. DEC-107 (a missing record is reported, not deleted; a
`PLAN.md`-only first pass is the pre-approved fallback).

## Changes made

- `PLAN.md` — every "**WI-nnn has now run and closed**" (and equivalent-phrasing) paragraph
  removed from §2, back through WI-028, once its `docs/completed/WI-nnn.md` was confirmed to
  exist (checked all ~114 ids via `ls docs/completed/`; none missing, so DEC-107's "reported,
  not deleted" branch was never triggered). Ordering/constraint notes bound entirely to a now-
  closed batch (the dice-batch `### Ordering and constraints`, the background/local-mode
  `### Schema versions in this batch`, the WI-124–137 hex-crawl/initiative batch narratives)
  went with their batch. The one still-relevant `## Triage addendum` section (WI-120 art
  review) was kept but trimmed: its three "Scheduled" bullets, all now closed, collapsed to one
  pointer sentence; its "Not scheduled: IN-117" line survives, since IN-117 is still Open.
  Ordering/constraint notes binding the still-open WI-144 – WI-151 batch (the "2026-09-18
  introspective" section) are untouched. 1,184 → 74 lines, then 73 after removing this item's
  own now-closed row from §2's table.
- `PLAN-COMPLETED.md` §3 — added the one-line WI-144 row (below).
- `INTAKE.md` — IN-140's row moved from §1.1 to §1.2, written in the new one-line "Closed —
  WI-nnn (date). See `docs/completed/WI-nnn.md`." shape rather than the old multi-sentence
  "Closed via" prose the rest of §1.2 still carries.

## Visible behavior changes

None (documentation only). A planning session opening `PLAN.md` now reads 73 lines instead of
1,184.

## How to verify

- `wc -l PLAN.md` — 73.
- `grep -n "has now run and closed" PLAN.md` — no matches.
- `pnpm verify` green (lint + typecheck + unit; nothing under `apps/`/`packages/` changed).
- Spot-check: every `WI-nnn` id no longer mentioned in `PLAN.md` has a
  `docs/completed/WI-nnn.md` (confirmed for the full WI-028–WI-143 range during execution).

## Deviations

**`PLAN-COMPLETED.md` §3 needed no change beyond adding this row.** Its ~115 existing rows are
already one line each — SPEC-052's "reduce to one line" was already true there, so that half of
the work item's stated scope was a no-op on inspection rather than an edit.

**`INTAKE.md` §1.2's ~120 existing "Closed via" cells were left unreduced — the pre-approved
`PLAN.md`-only fallback (DEC-107, gate note).** Each cell is a multi-sentence technical summary
duplicating its `docs/completed/WI-nnn.md`; trimming all of them to one line is materially the
same size of work as the `PLAN.md` pass just done, and DEC-107 explicitly pre-approved doing the
`PLAN.md` half first and recording the rest here rather than blocking this item on finishing
both in one session. This item's own `IN-140` row was written in the target one-line shape as a
model for that follow-up pass; the id is unchanged and stays Closed either way (RULE-019), so no
information is at risk of loss — the reduction is a compression of `INTAKE.md`, not a fact
relocation. Logged as **IN-151** (`INTAKE.md` §1.1) rather than left implicit.
