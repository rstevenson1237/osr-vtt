# WI-092 — `:focus-visible` ring on every shell icon control

**Spec:** SPEC-044 · **From:** IN-075 · **Agent:** `claude-code` (`sonnet`) · **Effort:**
small · **Closed:** 2026-08-31

**Changes made**

- `QuickSheetRail.svelte` — `.stoggle:focus-visible`. `MainViewTabs.svelte` —
  `.vtab:focus-visible` (covers rail, drawer, mobile variants). `ActivityDrawer.svelte` —
  `.current:focus-visible`. `MobileTopBar.svelte` — `.gear:focus-visible` (`.invite` is
  text-only, out of scope). `PresentationToggle.svelte` —
  `.presentation:focus-visible`. `SessionTab.svelte` — `.gear:focus-visible` (`.pill` is
  text-only, out of scope). `MapToolbar.svelte` — `button:focus-visible` on the existing
  bare `button` rule. `RoomShell.svelte` — `.logbtn:focus-visible, .logtab:focus-visible`
  (the one control here wrapping `Icon.svelte`; `.rail-move`/`.join-gate button` have no icon).
- Every added rule is the literal block SPEC-044 §1 specifies: `outline: 2px solid
  var(--focus); outline-offset: 2px`, on `:focus-visible` (never `:focus`), owned per
  component per SPEC-044 §2.
- No markup changed; no disabled-state styling added (SPEC-044 §3 — already correct).
- `README.md` — new "Focus (SPEC-044)" paragraph naming the eight components, the rule,
  and why `:focus-visible` rather than `:focus`.
- `PLAN.md`, `PLAN-COMPLETED.md`, `INTAKE.md` — ledger moves.

**Visible behavior changes** — tabbing to any icon-only shell control (quick-sheet rail,
view tabs, drawer trigger, mobile top bar's gear, fullscreen toggle, session tab's gear,
every map-tool button, Log button) now draws a visible 2px `--focus` ring, offset 2px
outside the border. Clicking/tapping draws no ring (`:focus-visible`, not `:focus`).
Nothing else about these controls changes.

**How to verify** — `pnpm dev`, Tab through the shell and confirm each shows the ring only
via keyboard, never on click; check a **Map** button per SPEC-044 §1's callout. `pnpm
verify` — lint/typecheck/unit (840 tests) clean. `pnpm verify:all` — full emulator suite
green, confirming the CSS-only change didn't disturb any testid or interaction.

**Deviations** — none. Stayed inside SPEC-044 §1–§2's eight named components; did not
touch the disabled state (§3) or `EncounterBoard.svelte`'s separate `--focus` use (§4).
