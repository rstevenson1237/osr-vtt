## WI-065 — `RULE-AMENDMENT`: RULE-010's economic premise under Blaze

**Spec:** SPEC-034 §1 · **From:** IN-037 · **Decision:** DEC-049 (answered (c), user,
2026-08-03) · **Closed:** 2026-08-14 · Standalone `RULE-AMENDMENT:` commit (RULE-017).

### Changes made

- **`RULES.md`** — RULE-010 rewritten. Heading is now "No Cloud Functions" (the billing
  card was the Spark-era mechanism for the premise, not the rule). The no-Cloud-Functions
  clause is restated and strengthened with what it forbids for want of a trusted writer.
  The single tier-independent economic premise is replaced by a **per-tier** statement:
  Spark unchanged (exhaustion denies rather than bills), Blaze replaced by three
  consequences — (1) per-write Security-Rules containment is the only in-app boundary and
  no aggregate quota is enforceable, client caps being friction; (2) App Check enforcement
  stops being optional; (3) the backstop is a Cloud Billing budget plus a `[HUMAN]`
  runbook, which warns rather than caps. Adds the DEC-049 note that the exposure is not
  only Storage — Firestore reads and RTDB bandwidth bill too, so RULE-012's "the roomId is
  the capability" is a cost lever on Blaze whether or not uploads ship. Closes with the
  standard `> **Amended by WI-065 (2026-08-14).**` block.
- **`docs/spec/SPEC-034.md`** — §1's blocking warning cleared; status records the
  amendment as landed, so WI-066 may begin.
- **`PLAN.md`** — WI-065 removed from §2 and added to the completed list; WI-066's gate
  cell no longer reads "Blocked on WI-065"; closure note added to the status narrative.
- **`PLAN-COMPLETED.md`** — §3 row added.
- **`docs/completed/WI-065.md`** — this file.

`INTAKE.md` §1.1's IN-037 row stays in §1.1: IN-037 covers WI-065 **and** WI-066, and
WI-066 has not landed.

### Visible behavior changes

**None.** Documentation only — no source file, schema, rule file or test was touched.

### How to verify

- `git show --stat` on the commit: `.md` files only, message prefixed `RULE-AMENDMENT:`
  (the `guard-protected-paths.sh` hook enforces the prefix on any `RULES.md` write).
- `RULES.md` → RULE-010 reads per-tier and carries the amendment block.
- `pnpm verify` — lint + typecheck + unit, unchanged and green.

### Deviations

- **RULE-009 left untouched.** It still names the Spark tier, which is correct today: the
  project has not upgraded, and SPEC-034 is written "ahead of a Blaze upgrade". The
  amended RULE-010 holds under both tiers, so no second rule needed changing — and
  widening the amendment beyond RULE-010's premise is exactly what DEC-049 (c) declined.
- **`campaign-store.ts:83`'s comment** ("a Cloud Function and a billing card") was left
  as-is: it is a source file, this is a standalone rule amendment (RULE-017), and the
  statement is still true as project history. Not worth an intake item.
