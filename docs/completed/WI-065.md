## WI-065 — `RULE-AMENDMENT`: RULE-010's economic premise under Blaze

**Spec:** SPEC-034 §1 · **From:** IN-037 · **Decision:** DEC-049 (answered (c), user,
2026-08-03) · **Closed:** 2026-08-14 · Standalone `RULE-AMENDMENT:` commit (RULE-017).

### Changes made

- **`RULES.md`** — RULE-010 rewritten. Heading is now "No Cloud Functions". The single
  tier-independent economic premise is replaced by a **per-tier** statement: Spark
  unchanged (exhaustion denies rather than bills), Blaze replaced by three consequences —
  (1) per-write Security-Rules containment is the only in-app boundary, no aggregate quota
  is enforceable; (2) App Check enforcement stops being optional; (3) the backstop is a
  Cloud Billing budget plus a `[HUMAN]` runbook, which warns rather than caps. Adds the
  DEC-049 note that Firestore reads/RTDB bandwidth bill too. Closes with the standard
  `> **Amended by WI-065 (2026-08-14).**` block.
- **`docs/spec/SPEC-034.md`** — §1's blocking warning cleared; status records the
  amendment as landed, so WI-066 may begin.
- **`PLAN.md`** — WI-065 removed from §2 and added to the completed list; WI-066's gate
  cell no longer reads "Blocked on WI-065"; closure note added to the status narrative.
- **`PLAN-COMPLETED.md`** — §3 row added.
- **`docs/completed/WI-065.md`** — this file.

`INTAKE.md` §1.1's IN-037 row stays: it covers WI-065 **and** WI-066, and WI-066 has not landed.

### Visible behavior changes

**None.** Documentation only — no source file, schema, rule file or test was touched.

### How to verify

- `git show --stat` on the commit: `.md` files only, message prefixed `RULE-AMENDMENT:`.
- `RULES.md` → RULE-010 reads per-tier and carries the amendment block.
- `pnpm verify` — lint + typecheck + unit, unchanged and green.

### Deviations

- **RULE-009 left untouched.** The project has not upgraded to Blaze, and the amended
  RULE-010 holds under both tiers, so no second rule needed changing.
- **`campaign-store.ts:83`'s comment** ("a Cloud Function and a billing card") was left
  as-is: this is a standalone rule amendment (RULE-017), and the statement is still true.
