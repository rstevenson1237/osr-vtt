# WI-142 — The session bootstrap

**Spec:** SPEC-053 §1 · **From:** IN-144 · **Agent:** `claude-code` · **Model:** `sonnet` ·
**Effort:** M · **Closed:** 2026-09-19

## Changes made

- `.claude/hooks/session-bootstrap.sh` (already landed, PR #191, commit `6c87eb2`) — a
  best-effort, idempotent `SessionStart` hook: `pnpm install --frozen-lockfile`, then
  `firebase setup:emulators:{firestore,database,storage,ui}` and Playwright's pinned
  Chromium (skipped if `resolveChromiumPath()` would already find one), with the proxy
  variables unset for those last two fetches only (README's proxy trap). It never edits a
  tracked file and never runs the suite itself.
- `.claude/settings.json` — the `SessionStart` registration pointing at the script (commit
  `b31db34`). This was the step DEC-106 gated: reading the `PreToolUse`/`SessionStart`
  hook count per event, not per file, so one more `SessionStart` entry beside the existing
  three `PreToolUse` guards stays within SPEC-053 §3's count.
- `CLAUDE.md`'s harness paragraph and `README.md`'s Dev commands section (both already
  landed with the above) describe the hook and its four constraints.
- This session: `docs/completed/WI-142.md` (this file), `PLAN-COMPLETED.md` §3 row added,
  the item removed from `PLAN.md` §2 and its stale blocked-note paragraph, `INTAKE.md`'s
  IN-144 row moved §1.1 → §1.2.

## Visible behavior changes

A fresh session now runs `pnpm install`, pre-fetches the Firebase emulator jars, and
resolves Playwright's Chromium automatically at startup, printing `[session-bootstrap]`
progress lines — confirmed live in this session's own `SessionStart` output. No change to
any command's behavior once run by hand.

## How to verify

Start a fresh session against this branch and confirm the `SessionStart` hook fires
(`[session-bootstrap] ...` lines) before the first turn. `pnpm verify` (lint + typecheck +
unit) is green. `pnpm verify:all` was exercised in this session — `firebase` was resolvable
and the run proceeded past the environment/tooling checks rather than reporting "could not
run" — but did not finish inside the session's practical time budget; the emulator battery
runs to completion in CI (`.github/workflows/ci.yml`), consistent with every other item in
this batch.

## Deviations

None new. The implementation (hook script + registration) had already landed on `main` in
PR #191 and a follow-up commit before this session started; this session's work was
finishing the record-keeping steps (step 7) that close the item — `docs/completed/WI-142.md`
did not yet exist, and `PLAN.md` §2 still carried it with a stale note describing the
registration as blocked, which is what this session found and cleared.
