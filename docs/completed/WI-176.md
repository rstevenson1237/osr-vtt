# WI-176 — Investigation: mobile Map tools (cancelled)

**From:** IN-170 (INT-UX-17). **Spec:** —. **Agent:** human + claude-code. **Model:** `sonnet`.
**Effort:** `low`. **Cancelled:** 2026-09-24, user request.

## What happened

WI-176's step 1 was a `[HUMAN]` phone playtest of the Map tools palette (`MapToolbar`,
~50 controls in a half-height phone bottom sheet), with the agent's role limited to turning
the playtest notes into new intake items. At the point of execution the playtest had not
been performed and no notes existed. Rather than leave the item open waiting on it
indefinitely, the user asked to close it out now.

## Changes made

Documentation only, no code:

- `PLAN.md` — WI-176 removed from §2 (upcoming work items) and from the WI-196 effort table;
  the investigation-order narrative updated to record the cancellation in place of "waits on
  the user's phone playtest."
- `INTAKE.md` — IN-170 moved from §1.1 (Open and scheduled) to §1.2 (Closed intake) with
  disposition **Cancelled**; its §1.1-era detail entry annotated in place per RULE-019 (the
  id is retired, not reused).
- `PLAN-COMPLETED.md` — WI-176 row added to §3, described as cancelled.
- This file.

## Visible behavior changes

None. No application code, schema, rules or UI changed.

## How to verify

`git grep -n "WI-176" PLAN.md INTAKE.md PLAN-COMPLETED.md` shows the item as closed/cancelled
everywhere it's referenced, and the WI-176 row is absent from `PLAN.md` §2's upcoming table.

## Deviations

None — this is a record-only cancellation, not an executed unit of work, so neither RULE-015
exception applies. If mobile Map tools usability is revisited later, it re-enters as a new
intake item rather than reopening IN-170/WI-176 (RULE-019: retired ids are never reused).
