# WI-157 — Hosted version + Report a problem

SPEC-054 §11. Part of Batch 1 (WI-152–158) — one pull request, one combined summary.

**Changes made**

- `SessionActivity.svelte` — a footer (hosted build only, `multiplayer` gate) renders
  `VITE_APP_VERSION` beside a **Report a problem** link to
  `https://github.com/rstevenson1237/osr-vtt/issues/new`, with `Version: <version>`
  prefilled into the issue body. No account menu exists in this shell to hang it off,
  so it lands in the Session settings footer instead (spec's named fallback).
  `local-app-version` (`LocalLobby.svelte`) is untouched.

**Visible behavior changes** — the hosted build's Session settings panel now shows its
build version and a report-a-problem link at the bottom.

**How to verify** — `pnpm verify:all` green. Manually: open Session settings on the
hosted build, confirm the version + link render at the bottom, and that the link opens
a pre-filled GitHub issue.

**Deviations** — none.
