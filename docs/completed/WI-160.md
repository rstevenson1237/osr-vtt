# WI-160 — Keys + Referee: rename the Room sheet; "Referee" in all copy

SPEC-054 §6. Part of Batch 2a (WI-160, WI-162, WI-163) — one pull request, one combined
summary.

**Changes made**

- `apps/web/src/lib/shell/activities.ts` — the `room` quick sheet's `title` changes from
  `'Room'` to `'Keys'`. The activity id (`'room'`) and icon are unchanged.
- `apps/web/src/lib/components/shell/RoomsPanel.svelte` (the sheet this title belongs to)
  — every user-facing string that means a map's labelled `MapRoom` entry (not the
  campaign room) now says **Key**/**Keys** instead of **Room**/**Rooms**: the panel
  heading, the empty-state hints, the delete-confirmation dialog, the per-row `title`
  tooltips (select/jump-to/delete), the edit-field `aria-label`s, the docked-sheet tip,
  and the "+ Add key" button. `data-testid`s (`room-*`), `isGM`, `MapRoom`, and every
  other code identifier are unchanged, per SPEC-054 §6.
- `apps/web/src/lib/components/shell/PlayersPanel.svelte` — the two-step transfer-referee
  confirmation dialog's messages say "Referee" instead of "GM" (`transferGM`,
  `gmUid` unchanged).
- `README.md` — "Room quick sheet & players' notes" renamed to "Keys quick sheet & players'
  notes", with a note on the Room/Key terminology split.

**Visible behavior changes** — the shell rail's "Room" sheet is now labelled "Keys", and
every string inside it (headings, hints, dialogs, button/field labels) says
"Key"/"Keys" rather than "Room"/"Rooms". The Session settings sheet's own "Room" section
(campaign name, invite link, etc.) is unchanged — "Room" in user-facing copy now means
only that. The referee-transfer confirmation dialog says "Referee" instead of "GM".

**How to verify** — `pnpm verify` green (lint, typecheck, unit). Manually: open the Keys
sheet (docked and expanded), confirm all copy reads "Key"/"Keys"; open Players and start a
transfer-referee action, confirm both confirmation messages say "Referee".

**Deviations** — none. Renaming the sheet's internal copy (not just its rail title) is
within SPEC-054 §6's stated scope ("'Room' in user-facing copy then means only the
campaign room"): leaving the panel's own headings/hints as "Room"/"Rooms" while the rail
said "Keys" would have been an inconsistent, half-done rename of the same sheet.
