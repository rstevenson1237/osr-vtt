## WI-071 — Ledger repairs

**Gate:** ✅ Cleared — user, 2026-08-11.

### Changes made

Three ledger repairs, all from the 2026-08-09 audit (IN-044, IN-045, IN-046):

1. **SPEC-028 index cell**: During the planning of WI-071 (2026-08-11), SPEC-028 was reopened a third time by IN-050 (free snap's vertex attraction, DEC-061), restoring its status to **Active**. The index cell in `SPEC.md` was already **Active**, so index and body now agree.

2. **Hex-grid "Deferred" entries**: Two stale entries in `DECISIONS.md` (the locked-defaults table row "Hex grid | Deferred" and the Postponed bullet "Hex grid. Deferred.") were annotated in place per RULE-019 during planning (2026-08-11). The annotations explain that the entries are stale: SPEC-030 (Hex Crawl map type) is now **Active**, IN-011 is **Scheduled**, and WI-037–WI-041 all carry cleared gates. The entries are retained, never deleted, with the stale status explained.

3. **IN-041's row move to §1.2**: IN-041 (Lobby credits, and the symbol pack's provenance) was moved from §1.1 (Open and scheduled) to §1.2 (Closed intake) in `INTAKE.md` during planning. WI-060 landed 2026-08-04 and closed this intake item; the row was overdue for the move.

No code changes were made — this is ledger-only work.

### Visible behavior changes

None. This is documentation maintenance.

### How to verify

1. **SPEC-028 status**: Verify `docs/spec/SPEC-028.md` line 3 reads "**Status: Active**" and `SPEC.md` line 77 shows "SPEC-028 | Snap-aware carve tool geometry | **Active**".
2. **Hex-grid entries**: Verify `DECISIONS.md`:
   - Line 204 in the locked-defaults table: "Hex grid" row is annotated "(IN-045, WI-071)" and explains it is stale.
   - Line 401 in the Postponed section: "Hex grid" bullet is annotated "(IN-045, WI-071)" and explains it is stale.
3. **IN-041 move**: Verify `INTAKE.md`:
   - §1.1 (Open and scheduled, lines 31–52) does not contain IN-041.
   - §1.2 (Closed intake, lines 54+) contains IN-041 with "Closed via WI-060 / SPEC-033 §6".

### Deviations

None.
