# WI-091 — Redraw the icon set under SPEC-043's depiction rule

**Spec:** SPEC-043 · **From:** IN-074 · **Decision:** DEC-076 (Direction A · Implement) ·
**Agent:** `claude-code` (`sonnet`) · **Effort:** medium · **Closed:** 2026-08-30

**Changes made**

- `apps/web/src/lib/components/shell/Icon.svelte` — the wrapping `<svg>`'s `stroke-width`
  moves from `1.8` to `1.75` (SPEC-043 §1). Three `MARKUP` entries redrawn per SPEC-043
  §4, the glyphs that failed at palette size:
  - `dice` — a true d20: three internal spokes run from centre to alternating outer
    vertices instead of a facet across the top.
  - `tools` — a latched toolbox (rounded rect body, arched handle, lid-seam, latch tab)
    replacing the diagonal chisel that merged head into shaft at 20px.
  - `ruler` — a straightedge lying flat (horizontal rounded rect, four graduation ticks)
    replacing the diagonal rhombus that read as a silhouette.
  Doc comment now points at SPEC-043's precedence rule instead of claiming verbatim R1
  tracing. The other 31 `MARKUP` entries are unchanged — only these three needed new
  geometry (RULE-015).
- `README.md` — new "Icon system (SPEC-043)" subsection states the technique and subject
  precedence rule; `tool-groups.ts` paragraph cross-references it.
- `PLAN.md`, `PLAN-COMPLETED.md`, `INTAKE.md` — ledger moves.

**Visible behavior changes** — three glyphs in the shell's icon set render differently:
the dice/roll icon, the Tools nav icon, and the map Ruler tool icon. Every icon in the
app is very slightly thinner (1.75px vs 1.8px stroke at the same 24×24 viewBox). No
control's position, size, colour, `data-testid`, or accessible name changes — `Icon.svelte`
still renders `aria-hidden="true"` and the label lives on the wrapping control, untouched
by this change.

**How to verify** — `pnpm dev` and look at: the Roll quick sheet dice icon, the shell's
Tools-labelled control, and the map tools palette's Ruler button. `pnpm verify` (lint +
typecheck + unit) — lint/typecheck clean; two pre-existing `*.emulator.test.ts` failures
are unrelated (confirmed against the unmodified branch; no Firebase emulator reachable in
this session). `pnpm verify:all` is what CI runs to exercise those tests.

**Deviations** — none. Scope stayed inside SPEC-043's 34 glyphs and the two README touch
points `PLAN.md` named; the 31 glyphs §4 did not name were left as-is (RULE-015).
