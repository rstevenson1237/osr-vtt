## WI-149 — `RULE-AMENDMENT`: RULE-015 gains a bounded Deviations budget

**Closed 2026-09-20.** Spec: — (rule). From: IN-147 / DEC-101 (answered **(b)**, user,
2026-09-18). Agent: `claude-code`, model `opus`, effort S. Standalone `RULE-AMENDMENT:`
change (RULE-017) — no implementation is bundled with it.

### Changes made

- **`RULES.md`** — RULE-015's "**The one exception**" paragraph becomes "**Two exceptions,
  and no others**": (1) the unchanged unblock exception, and (2) the **Deviations budget** —
  a defect fix taken on the executor's own initiative, under three conditions that all bind:
  in a file the work item already changes (not one merely read, nor one opened in order to
  make the fix), ≤ 20 lines, and shipping with a test added in the same change. Both
  exceptions are recorded under **Deviations**, and an unrecorded fix is restated as a
  violation. A fix that outgrows any one condition is the signal to stop and log an intake
  item. An amendment note in the RULE-006/RULE-009/RULE-016 style records what the rule
  previously said, the measured cost (WI-127, WI-128), DEC-101's answer with all three
  conditions binding rather than any two, what is traded away, and why option (c)'s
  any-file budget was rejected.
- **`CLAUDE.md`** — the §Standing rule "one exception" paragraph becomes the same
  two-exception list, with the three conditions and the "two of three is not the budget"
  line.
- **`.claude/commands/execute-wi.md`** — step 6 states both exceptions and the three
  conditions; the **Deviations** bullet in step 7 is named as the ledger for both, a budget
  fix to be named with the test that covers it.
- **`PLAN-COMPLETED.md`** — the completion-summary template's **Deviations** line covers
  both exceptions rather than only unblocking changes; the WI-149 row added to §3.
- **`docs/spec/SPEC-052.md`** — §3's parenthetical "(RULE-015 is unchanged)" was made false
  by this amendment and is replaced: the budget buys a tested **defect** fix, and a passage
  that is merely long is not a defect, so a prose trim is still unlicensed. The section's
  meaning is unchanged.
- **`DECISIONS.md` / `docs/decisions/DEC-101.md`** — DEC-101 closed out of §Open into its
  own entry file, with a §Closed index row.
- **`PLAN.md`** — WI-149 removed from §2; the batch's ordering note now records both rule
  amendments as landed.
- **`INTAKE.md`** — IN-147 moved from §1.1 to §1.2, closed via WI-149.

### Visible behavior changes

**None** in the application. The change is to the process rules: from now on an execution
session may fix a defect within the budget's three bounds without a separate chain, and
records it under **Deviations**.

### How to verify

- `RULES.md` → RULE-015 lists two exceptions, three binding conditions on the second, and
  carries a WI-149 amendment note. `CLAUDE.md` §Standing rule and
  `.claude/commands/execute-wi.md` step 6 say the same thing.
- `DECISIONS.md` §Open no longer contains DEC-101; `docs/decisions/DEC-101.md` exists and
  the §Closed index links it.
- `PLAN.md` §2 has no WI-149 row; `INTAKE.md` §1.2 has the IN-147 row.
- `pnpm verify` green (lint + typecheck + unit, `docs:check` included).
- `git log -1` carries the `RULE-AMENDMENT:` prefix the `PreToolUse` hook requires, and the
  commit contains no implementation change (RULE-017).

### Deviations

- **`docs/spec/SPEC-052.md` §3 touched beyond the work item's stated file list.** The item
  named RULES.md and CLAUDE.md; SPEC-052 §3 asserted "RULE-015 is unchanged" as a parenthetical,
  which this amendment falsifies. Correcting it is RULE-018 (docs move with the change), not a
  side fix — the section's own rule is unchanged, and the replacement text says why the budget
  does not reach a prose trim.
- **No Deviations-budget fix was taken in this work item.** The amendment is standalone
  (RULE-017) and carries no implementation, so there was no file in which the budget it
  creates could have been spent.
