## WI-072 — Ledger audit follow-through

**Changes made.**

- `PLAN-COMPLETED.md` §3 — removed three duplicate rows: the earlier-worded WI-058 and
  WI-059 rows (kept the later pair, which already sat in the right chronological spot next
  to WI-060/WI-061), and the WI-068 row whose description didn't match `docs/completed/WI-068.md`
  (kept the row matching the shipped change: `anchorCellFor` honours snap mode).
- `PLAN.md` §2 — removed the WI-072 row now that its gate has been executed; updated the
  execution-order line to drop WI-072 from the head of the chain.
- `INTAKE.md` — moved IN-047 and IN-048 from §1.1 (Scheduled) to §1.2 (Closed via WI-072),
  with a one-line disposition note each.

**Visible behavior changes.** None — documentation only.

**How to verify.** `grep -c "WI-058\|WI-059\|WI-068" PLAN-COMPLETED.md` shows one row per
id; `grep -n "IN-047\|IN-048" INTAKE.md` shows both only in §1.2.

**Deviations.** None. Investigation confirmed the WI-058/WI-059/WI-068 duplicate pairs each
described one real change (same commit, same PR, same closing date) rather than two, so
RULE-019's "fresh id for a second real change" branch didn't apply — a straight dedupe was
sufficient. SPEC-029 §2 verification found no gap: the rect (not raster) is stored, the
battle map renders background/floor/overlay clipped to the rect with no source grid
(`battle-map.ts`), and `exportPng` (`vector-engine.ts`) stays wired with
`backgroundColor` compositing via `compositeBackgroundColor`. IN-048 closed clean, no
follow-up item logged.
