# Supplied hex-symbol pack — landed by WI-101

**This directory is now empty.** It held the 37 `.svg` files the user supplied with
the 2026-09-02 hex-tools batch, parked here as intake material for IN-089 pending
provenance review (DEC-083). WI-101 (SPEC-047 §6) has since landed all 37 into
`apps/web/public/assets/hex/terrain/*.svg` and `apps/web/public/assets/hex/contents/*.svg`,
re-authored white and wired into `HEX_TERRAIN_CATALOG`/`HEX_CONTENTS_CATALOG`
(`packages/shared/src/map/hex/catalog.ts`). Provenance is recorded in
`apps/web/public/assets/ATTRIBUTION.md`.

See `docs/completed/WI-101.md` for the full file-to-kind mapping and what changed.
