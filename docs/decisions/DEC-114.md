## DEC-114 — The Edit/View choice lasts for the tab session

**Closed 2026-09-23** — answered by the user under `/work-item` (IN-156). **Supersedes
DEC-064 in part**: its one binary button stands; its "every freshly joined session starts in
View" is narrowed to "every new browser session".

- **Question.** DEC-064 reversed WI-053's `'edit'` default so that every freshly joined
  session lands under the View lock — a guard against a stray click at the start of play. The
  cost: a referee re-flips the lock after every reload or map switch. Remember the choice?
- **Answer.** **Remember it per room in `sessionStorage`** (user, 2026-09-23): a reload, a map
  switch or a view switch keeps the mode; a new browser session starts in View. Read and
  write are wrapped so a blocked storage falls back to View.
- **Impact.** README's Edit/View paragraph ("Defaults to `'view'`: every freshly joined
  session…") changes. Still per-viewer client state, not a store write; DEC-001 is untouched.
- **Alternatives.** Remembering per room indefinitely (full reversal of DEC-064); keeping
  DEC-064 unchanged with only WI-155's hint.
