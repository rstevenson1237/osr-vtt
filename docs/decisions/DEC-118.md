## DEC-118 — Render dirty-tracking is built only if a measurement says so

**Closed 2026-09-23** — answered by the user under `/work-item` (IN-175). **Closes the
Postponed watch item "Full-viewport-diff rendering optimizations"** either way.

- **Question.** `renderAll()` redraws every layer on every change, and a vertex drag rebuilds
  the LoS scene per pointer-move. The watch item said: re-evaluate if maps grow large or
  Chromebook playtests dip below budget. Build per-layer dirty tracking now?
- **Answer.** **Measure first, after WI-171** (user, 2026-09-23). Step 1 benchmarks a large
  dungeon (and a vertex drag on it) with the WI-122 bench against the stated frame budget.
  Step 2 — per-layer `invalidate()` with one rAF-coalesced flush, and no per-move LoS rebuild
  — is built **only if step 1 is over budget**, and lands after WI-171's extraction plan. The
  measured number is recorded in `README.md` whatever it is, and the Postponed entry is
  annotated with it.
- **Impact.** Step 2 is a render-pass change (`opus`). If step 1 is within budget, step 2's
  work item is denied at its own gate with the number as the reason.
- **Alternatives.** Build it without the gate; keep the watch item.
